완료된 작업 요약
1. 닉네임 중복 문제 해결
- server/service/ranking.go - GROUP BY nickname으로 닉네임당 최고 점수만 표시
2. 서버 측 점수 검증 추가 (3개 게임)
| 게임 | 검증 방식 |
|------|----------|
| Jump Runner | 플레이 시간 대비 점수 상한 검증 (60fps 기준) |
| Snake | 음식 먹을 때마다 서버에 보고, 먹기 횟수 기반 점수 |
| Memory Card | 서버에서 카드 배치 관리, 매칭 검증, 점수 계산 |
3. 새로 추가된 API 엔드포인트
POST /api/game/jumprunner/start, /end, /submit
POST /api/game/snake/start, /eat, /end, /submit
POST /api/game/memorycard/start, /match, /end, /submit
---
오라클 서버에서 할 작업
cd /path/to/mini-games
git pull
# Go 서버 재빌드
cd server
go build -o server .
# 서버 재시작 (기존 프로세스 종료 후)
./server
수동 작업 필요 없음 - DB 스키마 변경 없이 기존 테이블 그대로 사용합니다.